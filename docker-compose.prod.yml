services:
    api:
        build:
            target: production
        expose:
            - '4000'

    nginx:
        ports:
            - '80:80'
            - '443:443'
        volumes:
            - ./docker/nginx/default.prod.conf:/etc/nginx/conf.d/default.conf:ro
