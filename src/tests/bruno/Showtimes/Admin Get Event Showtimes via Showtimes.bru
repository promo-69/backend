meta {
  name: Admin Get Showtimes by Event ID
  type: http
  seq: 32
}

get {
  url: {{baseUrl}}/showtimes/admin?eventId={{eventId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

vars:pre-request {
  eventId: 1
}

script:pre-request {
  const baseUrl = bru.getEnvVar('baseUrl') || 'http://127.0.0.1:4000/api/v1';
  bru.setVar('baseUrl', baseUrl);
}
