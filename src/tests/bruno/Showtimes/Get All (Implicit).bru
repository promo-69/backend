meta {
  name: Get All Showtimes (Implicit)
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/showtimes
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

vars:pre-request {
  baseUrl: https://backend-jog6.onrender.com/api/v1/test
}

script:pre-request {
  const baseUrl = bru.getEnvVar('baseUrl') || 'https://backend-jog6.onrender.com/api/v1/test';
  bru.setVar('baseUrl', baseUrl);
}
