meta {
  name: Detalle Funcion por Sucursal
  type: http
  seq: 12
}

get {
  url: {{baseUrl}}/cinemas/{{cinemaId}}/showtimes/{{showtimeId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
