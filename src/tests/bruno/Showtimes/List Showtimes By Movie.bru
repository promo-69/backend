meta {
  name: Listar Funciones por Pelicula
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/showtimes?movieId={{movieId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

vars:pre-request {
  movieId: 1
}
