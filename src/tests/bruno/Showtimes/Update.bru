meta {
  name: Actualizar función (cambiar precio)
  type: http
  seq: 17
}

patch {
  url: {{baseUrl}}/showtimes/{{showtimeId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "price": 14.00
  }
}
