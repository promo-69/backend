meta {
  name: Get Cinema By ID
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/cinemas/{{testCinemaId}}
  body: none
  auth: none
}

script:pre-request {
  const baseUrl = bru.getEnvVar('baseUrl') || 'http://127.0.0.1:4000/api/v1';
  bru.setVar('baseUrl', baseUrl);
}
