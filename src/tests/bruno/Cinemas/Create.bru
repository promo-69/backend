meta {
  name: Create Cinemas
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/cinemas
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "example_field": "example_value"
  }
}
