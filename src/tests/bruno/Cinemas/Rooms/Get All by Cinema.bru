meta {
  name: Get All Rooms by Cinema
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/cinemas/{{testCinemaId}}/rooms
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

vars:pre-request {
  testCinemaId: 3
}

script:pre-request {
  const baseUrl = bru.getEnvVar('baseUrl') || 'http://127.0.0.1:4000/api/v1';
  bru.setVar('baseUrl', baseUrl);
}
