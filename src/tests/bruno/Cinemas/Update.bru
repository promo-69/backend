meta {
  name: Update Cinemas
  type: http
  seq: 4
}

put {
  url: {{baseUrl}}/cinemas/1
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "example_field": "updated_value"
  }
}
