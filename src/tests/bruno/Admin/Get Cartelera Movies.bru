meta {
  name: Get Cartelera Movies
  type: http
  seq: 9
}

get {
  url: {{baseUrl}}/admin/movies/cartelera
  body: none
  auth: bearer
}

auth:bearer {
  token: AT
}

script:pre-request {
  const baseUrl = bru.getEnvVar('baseUrl') || 'http://127.0.0.1:4000/api/v1';
  bru.setVar('baseUrl', baseUrl);
}
