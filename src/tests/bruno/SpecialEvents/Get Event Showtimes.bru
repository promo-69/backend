meta {
  name: Get Event Showtimes
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/special-events/{{eventId}}/showtimes?cinemaId={{cinemaId}}
  body: none
  auth: none
}

vars:pre-request {
  eventId: 1
  cinemaId: 1
}

script:pre-request {
  const baseUrl = bru.getEnvVar('baseUrl') || 'http://127.0.0.1:4000/api/v1';
  bru.setVar('baseUrl', baseUrl);
}
