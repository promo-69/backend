meta {
  name: Forgot Password
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/auth/forgot-password/{{userType}}
  body: json
  auth: none
}

body:json {
  {
    "email": "user@example.com"
  }
}

vars:pre-request {
  userType: employee
}
