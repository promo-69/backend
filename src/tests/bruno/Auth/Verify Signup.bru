meta {
  name: Verify Signup
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/auth/verify-signup
  body: json
  auth: none
}

body:json {
  {
    "email": "john.doe@example.com",
    "code": "123456"
  }
}
