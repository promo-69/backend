meta {
  name: Change User Status (Manager)
  type: http
  seq: 5
}

patch {
  url: {{baseUrl}}/users/1/status
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "status": 0
  }
}
