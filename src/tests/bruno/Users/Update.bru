meta {
  name: Update Users
  type: http
  seq: 4
}

put {
  url: {{baseUrl}}/users/1
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "exampleField": "updated_value"
  }
}
