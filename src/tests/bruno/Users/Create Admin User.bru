meta {
  name: Create Administrative User
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/users/admin
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "employee_id": {{testEmployeeId}},
    "role_id": 1,
    "email": "bruno-admin-user@example.com",
    "password": "Passw0rd!"
  }
}
