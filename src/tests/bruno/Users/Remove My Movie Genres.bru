meta {
  name: Remove My Movie Genres
  type: http
  seq: 1
}

delete {
  url: {{baseUrl}}/users/me/movie-genres
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "genres": [1]
  }
}
