meta {
  name: Update My Security
  type: http
  seq: 3
}

patch {
  url: {{baseUrl}}/users/me/security
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "currentPassword": "NewStrongPassword123!",
    "newPassword": "Pastor55.."
  }
}
