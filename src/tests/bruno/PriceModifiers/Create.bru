meta {
  name: Create PriceModifiers
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/price-modifiers
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "description": "Promoción martes",
    "modifierScope": 1,
    "operationType": 2,
    "isPercentage": true,
    "value": 20,
    "weekDay": 3
  }
}
