meta {
  name: Create PriceModifiers
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/price-modifiers
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "exampleField": "example_value"
  }
}
