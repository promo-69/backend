meta {
  name: Quote Order
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/v1/orders/quote
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "cinema": 1
  }
}
