meta {
  name: Quote Order - Admin
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/orders/quote
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "cinema": 1,
    "customerId": 1
  }
}

script:post-response {
  // Session is handled via user token now
}
