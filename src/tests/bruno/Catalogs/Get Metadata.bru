meta {
  name: Get Catalog Metadata
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/catalogs/{{catalog}}/metadata
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

vars:pre-request {
  catalog: roles
}
