meta {
  name: Get Catalog Items
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/catalogs/{{catalog}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

vars:pre-request {
  catalog: currencies
}
