meta {
  name: Update Seats
  type: http
  seq: 4
}

put {
  url: {{baseUrl}}/seats/1
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "example_field": "updated_value"
  }
}
