meta {
  name: Create Seats
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/seats
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "exampleField": "example_value"
  }
}
