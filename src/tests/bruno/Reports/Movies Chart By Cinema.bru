meta {
  name: Movies Chart By Cinema
  type: http
  seq: 22
}

get {
  url: {{baseUrl}}/reports/cinemas/{{cinemaId}}/movies/chart
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

params:query {
  from: {{fromDate}}
  to: {{toDate}}
}

vars:pre-request {
  cinemaId: 1
}

docs {
  Datos normalizados para gráfico de películas de una sucursal específica.
  Permiso: CRUD:READ:REPORTS-ALL
}
