meta {
  name: Movies Chart By Cinema
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/reports/movies/chart?from={{fromDate}}&to={{toDate}}&cinemaId={{cinemaId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

params:query {
  from: {{fromDate}}
  to: {{toDate}}
  cinemaId: {{cinemaId}}
}

vars:pre-request {
  cinemaId: 1
}

docs {
  Datos normalizados para gráfico de películas de una sucursal específica.
  Permiso: CRUD:READ:REPORTS-CHARTS
}
