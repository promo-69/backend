meta {
  name: Showtimes Chart By Cinema
  type: http
  seq: 25
}

get {
  url: {{baseUrl}}/reports/cinemas/{{cinemaId}}/showtimes/chart
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

params:query {
  from: {{fromDate}}
  to: {{toDate}}
}

vars:pre-request {
  cinemaId: 1
}

docs {
  Datos normalizados para gráfico de funciones de una sucursal específica.
  Permiso: CRUD:READ:REPORTS-ALL
}
