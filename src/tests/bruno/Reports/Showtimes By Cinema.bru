meta {
  name: Showtimes By Cinema
  type: http
  seq: 24
}

get {
  url: {{baseUrl}}/reports/showtimes?from={{fromDate}}&to={{toDate}}&cinemaId={{cinemaId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

params:query {
  from: {{fromDate}}
  to: {{toDate}}
  cinemaId: {{cinemaId}}
}

vars:pre-request {
  cinemaId: 1
}

docs {
  Reporte de funciones de una sucursal específica.
  Permiso: CRUD:READ:REPORTS-SHOWTIMES
}
