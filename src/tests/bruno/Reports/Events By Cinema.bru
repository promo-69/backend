meta {
  name: Events By Cinema
  type: http
  seq: 20
}

get {
  url: {{baseUrl}}/reports/events?from={{fromDate}}&to={{toDate}}&cinemaId={{cinemaId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

params:query {
  from: {{fromDate}}
  to: {{toDate}}
  cinemaId: {{cinemaId}}
}

vars:pre-request {
  cinemaId: 1
}

docs {
  Reporte de eventos de una sucursal específica.
  Permiso: CRUD:READ:REPORTS-EVENTS
}
