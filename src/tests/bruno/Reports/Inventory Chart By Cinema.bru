meta {
  name: Inventory Chart By Cinema
  type: http
  seq: 10
}

get {
  url: {{baseUrl}}/reports/inventory/chart?from={{fromDate}}&to={{toDate}}&cinemaId={{cinemaId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

params:query {
  from: {{fromDate}}
  to: {{toDate}}
  cinemaId: {{cinemaId}}
}

vars:pre-request {
  cinemaId: 1
}

docs {
  Datos normalizados para gráfico de inventario de una sucursal específica.
  Permiso: CRUD:READ:REPORTS-CHARTS
}
