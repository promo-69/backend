meta {
  name: Create Movies
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/movies
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "example_field": "example_value"
  }
}
